# CLAUDE.md

> 프로젝트명(가칭): **AppCompass AI**
>
> 목적: 앱 아이디어를 자동으로 구조화하고, 문제·타깃·해결책·MVP·검증 계획을 진단한 뒤 실제 근거에 따라 유지·수정·피벗을 제안하는 앱 기획 의사결정 시스템

---

## 0. 이 파일의 역할

이 문서는 Claude Code 및 AI 코딩 에이전트가 이 저장소에서 작업할 때 반드시 따라야 하는 최상위 프로젝트 지침이다.

구현 전에 항상 다음을 확인한다.

1. 지금 만드는 기능이 사용자의 실제 의사결정을 돕는가?
2. AI의 추측과 실제 근거가 구분되어 있는가?
3. 결과가 단순한 아이디어 나열이 아니라 실행 가능한 다음 행동으로 연결되는가?
4. 두 도메인 앱에 공통으로 적용되는 기능인지, 특정 도메인 전용 기능인지 구분되어 있는가?
5. 변경한 기능에 대한 테스트와 문서가 함께 수정되었는가?
6. 원본은 앱기획_4권_통합요약본.html 참고
---

# 1. 제품 비전

AppCompass AI는 단순한 기획서 생성기가 아니다.

```text
아이디어 입력
→ 문제·타깃 구조화
→ 위험요소 진단
→ 해결책 및 MVP 제안
→ 검증 실험 설계
→ 실제 데이터 수집
→ 유지·수정·피벗 판단
→ 새 기획안 생성
```

시스템의 핵심 역할은 정답을 대신 결정하는 것이 아니라 다음을 반복 가능하게 만드는 것이다.

- 불명확한 아이디어를 구조화한다.
- 타깃이 지나치게 넓거나 잘못 정의된 부분을 발견한다.
- 부족한 근거와 핵심 언노운(Unknown)을 표시한다.
- 검증 가능한 가설과 실험을 제안한다.
- 실제 사용자 근거를 반영해 판단을 갱신한다.
- 이전 기획 버전과 현재 기획 버전의 변경 이유를 추적한다.

---

# 2. 제품 원칙

## 2.1 문제 우선

화면이나 기능보다 먼저 다음 문장이 명확해야 한다.

```text
[사용자]는
[특정 상황]에서
[구체적인 문제]를 겪는다.

현재 [대체 방법]을 사용하지만
[시간·복잡성·실패·불안·비용] 때문에
문제가 충분히 해결되지 않는다.

우리 제품은 [핵심 행동]을 통해
[측정 가능한 변화]를 제공한다.
```

문제 상황이 없는 기능 아이디어는 낮은 완성도로 평가한다.

## 2.2 타깃은 행동으로 정의

다음 표현은 자동 경고 대상이다.

- 모든 사람
- 누구나
- 전 국민
- 학생 전체
- AI에 관심 있는 사람
- 수학을 배우는 아이
- 개발을 배우고 싶은 사람

나이와 직업만으로 타깃을 정의하지 않는다.  
반드시 **상황 + 문제 + 현재 행동 + 중단 원인**을 포함한다.

## 2.3 사용자·구매자·영향자 분리

특히 교육 앱은 다음 역할을 분리한다.

```text
사용자: 실제로 앱을 사용하는 사람
구매자: 결제하거나 설치를 결정하는 사람
영향자: 사용을 추천하거나 관리하는 사람
```

## 2.4 AI 판단은 가설

AI 출력에는 반드시 다음을 표시한다.

- 판단 결과
- 판단 근거
- 근거의 출처
- 근거 신뢰도
- 부족한 정보
- 다음 검증 실험
- 사람이 최종 확인해야 할 항목

근거가 부족하면 `HOLD`를 우선한다.

## 2.5 MVP는 가설 검증 도구

MVP 범위는 `P0 + 최소한의 P1`을 기본으로 한다.

```text
P0: 핵심 문제를 해결하는 기능
P1: 핵심 행동 완료율을 높이는 기능
P2: 재방문을 만드는 기능
P3: 편의·장식·확장 기능
```

## 2.6 측정 없는 추천 금지

추천 기능은 다음 중 하나 이상의 지표와 연결되어야 한다.

- 활성화
- 핵심 행동 완료
- 오류 감소
- 재방문
- 결제
- 공유
- 학습 전이
- 사용자 만족

---

# 3. 지원 제품

AppCompass AI의 공통 진단 엔진 위에 도메인 모듈을 연결한다.

```text
AppCompass Core
├─ VibeQuest Domain
└─ examath Domain
```

---

# 4. 도메인 1: VibeQuest

## 4.1 제품 정의

AI 코딩 도구로 첫 프로젝트를 만드는 과정에서 개발·AI 용어 때문에 진행이 중단되는 비개발자와 초보 개발자를 위한 실전형 용어 학습 게임.

## 4.2 금지되는 타깃 표현

```text
바이브코딩에 관심 있는 모든 사람
AI에 관심 있는 사람
개발을 배우고 싶은 사람 전체
```

## 4.3 1차 권장 타깃

```text
AI 코딩 도구로 처음 앱이나 자동화 프로그램을 만들고 있지만,
API, DB, Git, 토큰, 프롬프트, 에이전트 같은 용어를 이해하지 못해
작업이 자주 중단되는 비개발자와 초보 개발자
```

## 4.4 핵심 가치

```text
바이브코딩 중 막히는 용어를
실제 작업 상황과 연결된 3분 미션으로 해결한다.
```

## 4.5 MVP 범위

### 포함

- 게스트 즉시 시작
- 짧은 수준 진단
- 하루 3분 미션
- 실제 상황형 문제
- 틀린 개념 자동 복습
- 프로젝트 단계별 용어 지도
- XP, 보석, 콤보, 도감, 기본 퀘스트
- 학습 결과와 약점 요약

### 제외

- 기본 십자낱말
- 유료 루트박스
- 실시간 PvP
- 실시간 전체 랭킹
- 복잡한 길드
- 검증되지 않은 생성형 AI 자유채점
- 사용자의 프로젝트 코드를 무단 저장하는 기능

## 4.6 권장 문제 유형

- 정의와 실제 상황 연결
- 올바른 도구 선택
- 잘못된 AI 답변 찾기
- 코드·화면·오류와 용어 연결
- 유사 개념 차이 구분
- 작업 순서 배열
- 시나리오형 4지선다
- 짧은 주관식 후 키워드 채점

## 4.7 핵심 위험

- 일반적인 퀴즈 앱으로 보이는 문제
- 용어 암기만 되고 실제 작업에 전이되지 않는 문제
- 초보자와 현업 개발자의 난이도 충돌
- AI가 만든 부정확한 문제
- 너무 많은 게임 요소로 학습 흐름이 끊기는 문제

---

# 5. 도메인 2: examath

## 5.1 제품 정의

뺄셈, 특히 받아내림에서 막혀 수학 자체를 회피하기 시작하는 어린이가 수를 직접 조작하며 개념을 이해하도록 돕는 초등 수학 학습 앱.

## 5.2 1차 권장 타깃

```text
초등학교 2학년 중
두 자리 수에서 한 자리 수를 빼는 받아내림을 피하거나,
손가락 세기에서 다음 단계로 넘어가지 못하는 어린이
```

## 5.3 역할 분리

```text
사용자: 초등학교 2학년 어린이
구매자: 부모
영향자: 초등교사, 학원강사, 돌봄교사
```

## 5.4 핵심 가치

```text
수를 쪼개고, 옮기고, 덜어내는 행동을 통해
받아내림의 의미를 이해하게 만드는 오늘의 3분 뺄셈
```

## 5.5 진단해야 할 오류 유형

- 뺄셈의 의미를 모름
- 뒤로 세기가 어려움
- 수의 크기를 비교하지 못함
- 10을 만들고 쪼개지 못함
- 십의 자리와 일의 자리를 혼동함
- 받아내림의 의미를 이해하지 못함
- 세로셈 절차만 외움
- 틀릴까 봐 시도하지 않음
- 구체물에서는 성공하지만 숫자 문제로 전이하지 못함

## 5.6 MVP 범위

### 포함

- 초등 2학년 받아내림 집중
- 짧은 진단
- 구체물 조작
- 10 만들기와 쪼개기
- 구체물 → 그림 → 숫자 전환
- 한 화면에 한 가지 판단
- O/X, 매칭, 스와이프, 드래그
- 오답 유형 자동 분류
- 즉각적이고 비처벌적인 피드백
- 부모용 간단 주간 요약

### 제외

- 광고
- 가챠
- 실시간 랭킹
- 공개 채팅
- 과도한 경쟁
- 어린이에게 불필요한 회원가입 정보
- 정답률만으로 아이를 평가하는 기능
- 수학 전 범위를 한 번에 포함하는 기능

## 5.7 핵심 위험

- 일반 연산 문제집 앱으로 보이는 문제
- 애니메이션은 많지만 개념 이해는 없는 문제
- 구체물 문제에서 숫자 문제로 전이되지 않는 문제
- 부모가 원하는 결과와 아이가 느끼는 경험의 충돌
- 실패 표현이 수학 불안을 강화하는 문제
- 어린이 데이터 과수집

---

# 6. 공통 진단 결과 상태

시스템의 최종 판단은 다음 Enum을 사용한다.

```text
KEEP
REFINE
TARGET_PIVOT
PROBLEM_PIVOT
SOLUTION_PIVOT
CHANNEL_PIVOT
REVENUE_PIVOT
RETENTION_REDESIGN
HOLD
```

## 판단 원칙

- 근거 신뢰도가 낮으면 `HOLD`
- 문제는 강하지만 타깃이 넓으면 `TARGET_PIVOT`
- 문제 자체의 중요도가 낮으면 `PROBLEM_PIVOT`
- 문제는 강하지만 핵심 행동이 완료되지 않으면 `SOLUTION_PIVOT`
- 핵심 행동은 완료되지만 재방문이 낮으면 `RETENTION_REDESIGN`
- 유지율은 좋지만 유입이 약하면 `CHANNEL_PIVOT`
- 사용은 많지만 지불 의사가 낮으면 `REVENUE_PIVOT`

---

# 7. 에이전트 작업 순서

기능 구현 요청을 받으면 다음 순서로 작업한다.

## 7.1 요구사항 확인

1. 공통 엔진 기능인지 도메인 전용 기능인지 판단
2. 사용자, 구매자, 영향자 확인
3. 입력과 출력 확인
4. 성공 기준 확인
5. 실패·빈 상태·권한 거부 상태 확인
6. 분석 이벤트 확인
7. 테스트 기준 확인

## 7.2 구현 계획 작성

코드를 수정하기 전에 다음을 짧게 정리한다.

```text
목적
변경 파일
데이터 모델 영향
API 영향
UI 영향
분석 이벤트
테스트
마이그레이션 필요 여부
```

## 7.3 구현

- 작은 단위로 변경
- 도메인 로직은 서비스 계층에 배치
- LLM 호출과 규칙 엔진을 분리
- JSON 스키마 검증 필수
- 사용자 입력을 프롬프트 지시문으로 직접 연결하지 않음
- 점수와 피벗 판정은 재현 가능해야 함
- 모든 AI 응답에는 모델·프롬프트 버전 기록

## 7.4 검증

- 단위 테스트
- API 계약 테스트
- LLM 구조 출력 테스트
- 두 도메인 고정 시나리오 테스트
- 회귀 테스트
- 문서 업데이트

---

# 8. 권장 저장소 구조

```text
appcompass-ai/
├─ CLAUDE.md
├─ TECHSPEC.md
├─ README.md
├─ backend/
│  ├─ config/
│  ├─ apps/
│  │  ├─ accounts/
│  │  ├─ projects/
│  │  ├─ analysis/
│  │  ├─ evidence/
│  │  ├─ experiments/
│  │  ├─ reports/
│  │  ├─ analytics/
│  │  └─ domains/
│  │     ├─ vibequest/
│  │     └─ examath/
│  ├─ services/
│  │  ├─ llm/
│  │  ├─ scoring/
│  │  ├─ pivot/
│  │  └─ exports/
│  └─ tests/
├─ frontend/
│  ├─ src/
│  │  ├─ features/
│  │  ├─ pages/
│  │  ├─ components/
│  │  ├─ api/
│  │  └─ analytics/
│  └─ tests/
├─ prompts/
│  ├─ idea_structure/
│  ├─ diagnosis/
│  ├─ target_candidates/
│  ├─ mvp_plan/
│  └─ pivot_report/
├─ schemas/
├─ fixtures/
│  ├─ vibequest/
│  └─ examath/
└─ docs/
   ├─ decisions/
   ├─ research/
   └─ operations/
```

---

# 9. 코딩 규칙

## Python

- 타입 힌트 사용
- 비즈니스 로직을 View에 작성하지 않음
- 서비스 함수는 입력과 출력 타입이 명확해야 함
- 점수 계산은 순수 함수로 구현
- 시간은 timezone-aware 값 사용
- Enum과 상수를 문자열로 흩뿌리지 않음
- 데이터 변경 작업은 transaction 사용
- 외부 API 호출은 timeout, retry, idempotency 고려
- 로그에 민감정보를 기록하지 않음

## API

- `/api/v1/` 버전 사용
- 오류 응답 형식 통일
- 비동기 분석은 작업 ID 반환
- 재시도 가능한 작업은 idempotency key 지원
- 페이지네이션 적용
- 모든 분석 결과에 생성 시각과 버전 포함

## Frontend

- 화면 하나에는 하나의 주목적
- 로딩, 오류, 빈 상태를 반드시 구현
- 색만으로 상태를 구분하지 않음
- 어린이 화면은 읽기 부담과 선택지를 최소화
- 분석 결과 화면은 점수보다 이유와 다음 행동을 우선 표시
- 파괴적 작업은 확인 단계 제공

---

# 10. LLM 사용 규칙

## 10.1 허용

- 자유 입력 구조화
- 문제 정의 재작성
- 타깃 후보 생성
- 언노운 생성
- 실험 아이디어 생성
- 보고서 자연어 작성

## 10.2 금지

- 근거 없이 최종 사업성 확정
- 점수 산식을 LLM만으로 계산
- 피벗 상태를 자연어만으로 결정
- 어린이에게 부정적 낙인 부여
- 검증되지 않은 교육 효과 단정
- 출처 없는 시장 수치 생성
- 스키마 검증 없이 결과 저장

## 10.3 출력 계약

LLM 응답은 반드시 JSON Schema를 통과해야 한다.

실패 시:

1. 1회 자동 복구 프롬프트
2. 재실패 시 분석 상태를 `FAILED_SCHEMA`
3. 사용자에게 원인을 표시
4. 원본 응답은 보안 저장소에 제한적으로 보관
5. 실패 응답으로 점수나 피벗 결정을 만들지 않음

---

# 11. 근거 신뢰도

기본 근거 가중치는 설정값으로 관리한다.

```text
FOUNDER_ASSUMPTION = 0.20
DESK_RESEARCH      = 0.35
USER_INTERVIEW     = 0.50
PROTOTYPE_TEST     = 0.70
BEHAVIOR_DATA      = 1.00
```

이 값은 초기 정책이며 관리자 화면에서 변경 가능해야 한다.

AI는 근거를 생성하지 않는다.  
AI는 등록된 근거를 요약하고 해석한다.

---

# 12. 테스트 기준

최소 필수 테스트:

- “모든 사람”을 넓은 타깃으로 감지
- 사용자·구매자·영향자 분리
- 점수 가중치 합계 검증
- 근거 신뢰도 계산 검증
- `HOLD` 우선 규칙 검증
- 분석 결과 JSON Schema 검증
- VibeQuest 고정 입력 결과 회귀 테스트
- examath 고정 입력 결과 회귀 테스트
- 권한 없는 프로젝트 접근 차단
- 보고서 버전 추적
- 동일 idempotency key 중복 처리 방지

---

# 13. 완료의 정의

기능은 다음 조건을 모두 충족해야 완료로 본다.

- 요구사항 충족
- 정상 흐름 구현
- 오류·빈 상태 구현
- 권한 검증
- 분석 이벤트 기록
- 테스트 통과
- 마이그레이션 검증
- TECHSPEC 업데이트
- 사용자에게 보이는 문구 검토
- 어린이 관련 기능은 개인정보 최소화 검토
- AI 결과에 근거·신뢰도·다음 행동 표시

---

# 14. 문서 근거

이 프로젝트의 제품 원칙은 다음 자료의 공통 구조를 기반으로 한다.

- `기획의정석.pdf`: 문제 정의, Why, Whom, 핵심 메시지, 흐름
- `10년차IT기획자의노트.pdf`: 요구사항, 스펙, 협업, 백로그, 운영, 회고
- `앱기획편.pdf`: UX/UI, IA, 사용자 흐름, 와이어프레임, 출시
- `모바일앱마케팅.pdf`: 유입, 전환, 참여, 딥링크, 측정과 개선

특정 교육 교수법, 어린이 학습 안전, 도메인별 오류 분류는 별도 도메인 전문 모듈로 관리한다.

---

# 15. 최종 지침

```text
AI가 정답을 결정하지 않는다.

AI는
문제를 구조화하고,
부족한 근거를 찾고,
검증할 실험을 제안하고,
실제 데이터에 따라
유지·수정·피벗을 판단하도록 돕는다.
```
